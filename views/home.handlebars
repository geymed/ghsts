<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>GitHub Status</title>
</head>
<body>

    <h1>GitHub Status</h1>

    <p>Current status : {{status.status}}</p>

    <h3>Activity</h3>
    <ul>
        {{#each messages}}
        <li>
            <div>{{datify this.created_on}} : {{this.status}}</div>
            <div>{{this.body}}</div>
        </li>
        {{/each}}
    </ul>

</body>